using System.Data.Entity;

namespace DoubleJ.Oms.Model
{
    public class OmsDbContextInitializer : CreateDatabaseIfNotExists<OmsContext>
    {
        /// <summary>
        /// The seed.
        /// </summary>
        /// <param name="context">
        /// The context.
        /// </param>
        protected override void Seed(OmsContext context)
        {
            // Seed code here

            // var newEvent = new Event
            // {
            // Email = "alice@contoso.com",
            // BookedDate = DateTime.Now,
            // Room = "Onyx",
            // EventName = "Aniversary",
            // StartTime = new TimeSpan(10,0,0).ToString(),
            // EndTime = new TimeSpan(11, 0, 0).ToString(),
            // CreatedByEmail = "bob@contoso.com",
            // EventManager = "Gjiro"
            // };
            // context.Events.Add(newEvent);

            // Keep well-known sessions in separate room from generated sessions
        }
    }
}
